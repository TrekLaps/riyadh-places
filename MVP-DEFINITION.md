# 📱 MVP — وين نروح بالرياض
# التعريف الكامل + البنية التحتية + آلية العمل + Workflow

*تاريخ: 2026-02-20*

---

## 🎯 الـ MVP بالضبط — وش نبني؟

### القيمة الأساسية (Core Value Proposition)
**"أول منصة سعودية تقارن أسعار التوصيل + تعرض أسعار حقيقية + بحث AI بالعربي السعودي"**

ما أحد بالسعودية يسوي هالثلاث أشياء مع بعض.

### شاشات الـ MVP (5 شاشات فقط)

```
┌─────────────────────────────────────────────────┐
│                   الشاشات                        │
├─────────────────────────────────────────────────┤
│                                                  │
│  1. 🏠 Home — اكتشف + أماكن قريبة + ترند        │
│  2. 🔍 Search — بحث AI بالعربي + فلاتر           │
│  3. 📍 Place Detail — أسعار + مقارنة توصيل        │
│  4. 🛵 Delivery Compare — قارن 6 تطبيقات         │
│  5. ❤️ Favorites — المفضلة + قوائم مخصصة         │
│                                                  │
│  + Tab: 🗺 Map (خريطة تفاعلية)                  │
│  + Tab: 👤 Profile                               │
│                                                  │
└─────────────────────────────────────────────────┘
```

### الميزات الأساسية (MVP Features فقط)

| الميزة | الأولوية | لماذا؟ |
|--------|---------|--------|
| **مقارنة أسعار التوصيل** | P0 | الميزة القاتلة — أول واحد بالسعودية |
| **أسعار حقيقية بالريال** | P0 | ما أحد يسويها — كلهم يحطون $$$ |
| **بحث AI بالعربي** | P0 | "أبي مطعم ياباني رخيص بالعليا" |
| **خريطة + GPS** | P0 | "أماكن قريبة مني" |
| **فلاتر (ميزانية/فئة/مسافة)** | P0 | أساسي للتصفح |
| **صفحة مكان مفصّلة** | P0 | أسعار + صور + تقييم + موقع |
| **Dark/Light mode** | P1 | تجربة مستخدم |
| **مفضلة** | P1 | إعادة الزيارة |
| **"Perfect For" tags** | P1 | رومانسي، عوائل، شباب، عمل |
| **مقارنة عطور** | P1 | فئة جديدة — تفاصيل أدناه |

### ميزات مؤجلة (بعد الـ MVP)

- تسجيل دخول / حسابات
- تقييمات مستخدمين
- Social features (مشاركة، متابعة)
- Push notifications
- WhatsApp bot
- Check-ins / Gamification
- حجز مطاعم

---

## 🏗️ البنية التحتية (Infrastructure)

### المرحلة 1: MVP (الآن → أول إطلاق)

```
┌─────────────────────────────────────────────────────────┐
│                    MVP Architecture                       │
│                                                          │
│  ┌──────────┐    ┌──────────┐    ┌──────────────────┐   │
│  │  Website  │    │ iOS App  │    │  Admin/Scraper   │   │
│  │  (Static) │    │ (SwiftUI)│    │  (Python/Node)   │   │
│  └────┬─────┘    └────┬─────┘    └────────┬─────────┘   │
│       │               │                    │              │
│       ▼               ▼                    ▼              │
│  ┌────────────────────────────────────────────────┐      │
│  │              Supabase (Free Tier)               │      │
│  │  ┌──────────┐  ┌───────┐  ┌───────────────┐   │      │
│  │  │PostgreSQL │  │ Auth  │  │  Edge Functions│   │      │
│  │  │+ PostGIS  │  │(JWT)  │  │  (API Logic)  │   │      │
│  │  └──────────┘  └───────┘  └───────────────┘   │      │
│  │  ┌──────────┐  ┌───────────────────────────┐   │      │
│  │  │ Storage  │  │  Real-time (WebSocket)    │   │      │
│  │  │ (Images) │  │  (Live updates)           │   │      │
│  │  └──────────┘  └───────────────────────────┘   │      │
│  └────────────────────────────────────────────────┘      │
│                          │                                │
│                          ▼                                │
│  ┌────────────────────────────────────────────────┐      │
│  │           Cloudflare (Free Tier)                │      │
│  │  CDN + DNS + DDoS Protection + Edge Cache      │      │
│  └────────────────────────────────────────────────┘      │
│                                                          │
│  ┌────────────────────────────────────────────────┐      │
│  │           GitHub Actions (Free)                 │      │
│  │  CI/CD + Scraping Crons + Data Validation      │      │
│  └────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────┘
```

### لماذا Supabase؟

| المقارنة | Supabase | Firebase | Railway | AWS |
|----------|----------|----------|---------|-----|
| **Free tier** | ✅ 500MB DB + 1GB storage | ✅ محدود | ❌ $5/mo | ❌ معقد |
| **PostgreSQL** | ✅ كامل + PostGIS | ❌ NoSQL | ✅ | ✅ |
| **Arabic search** | ✅ Full-text + extensions | ❌ ضعيف | ✅ | ✅ |
| **Auth built-in** | ✅ | ✅ | ❌ | ❌ |
| **Real-time** | ✅ WebSocket | ✅ | ❌ | ❌ |
| **Storage** | ✅ 1GB free | ✅ 5GB | ❌ | ✅ |
| **Edge Functions** | ✅ (Deno) | ✅ (Node) | ✅ | ✅ |
| **Swift SDK** | ✅ رسمي | ✅ | ❌ | ❌ |
| **التكلفة MVP** | **$0/mo** | $0/mo | $5/mo | $20+/mo |
| **التكلفة Growth** | $25/mo | $25/mo | $20/mo | $50+/mo |

**القرار: Supabase** — PostgreSQL مجاني + Auth + Storage + Real-time + Swift SDK

### التكاليف المتوقعة

| المرحلة | الشهري | التفاصيل |
|---------|--------|----------|
| **MVP** | **$0** | Supabase Free + Cloudflare Free + GitHub Free |
| **Growth** (1K-10K users) | **$25** | Supabase Pro |
| **Scale** (10K-100K users) | **$75-150** | Supabase Pro + Cloudflare Pro + CDN |
| **Production** (100K+) | **$300-500** | Dedicated DB + Redis + Search engine |

---

## 🔄 الـ Workflow — كيف التطبيق يشتغل

### Data Flow (تدفق البيانات)

```
┌─────────────────────────────────────────────────────┐
│                   DATA PIPELINE                      │
│                                                      │
│  Sources:                                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │HungerStn │ │  Jahez   │ │  Keeta   │            │
│  │  API/Web  │ │ API/Web  │ │ API/Web  │ ...×6     │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘            │
│       │             │            │                    │
│       ▼             ▼            ▼                    │
│  ┌────────────────────────────────────────┐          │
│  │         Scraper (Python/Node)          │          │
│  │  - Cron: every 6 hours                 │          │
│  │  - Validate prices                     │          │
│  │  - Detect changes                      │          │
│  │  - Anti-bot rotation                   │          │
│  └──────────────┬─────────────────────────┘          │
│                 │                                     │
│                 ▼                                     │
│  ┌────────────────────────────────────────┐          │
│  │         Supabase PostgreSQL            │          │
│  │                                         │          │
│  │  Tables:                                │          │
│  │  - places (3,200+ → 5,000+)            │          │
│  │  - menus (items + prices)              │          │
│  │  - delivery_prices (6 apps)            │          │
│  │  - categories                           │          │
│  │  - neighborhoods                        │          │
│  │  - perfume_shops (جديد!)               │          │
│  │  - comparisons (مقارنات)               │          │
│  └──────────────┬─────────────────────────┘          │
│                 │                                     │
│        ┌───────┴───────┐                             │
│        ▼               ▼                              │
│  ┌──────────┐   ┌──────────┐                         │
│  │  Website  │   │ iOS App  │                         │
│  │  (API)    │   │ (SDK)    │                         │
│  └──────────┘   └──────────┘                         │
└─────────────────────────────────────────────────────┘
```

### User Flow (رحلة المستخدم)

```
المستخدم يفتح التطبيق
        │
        ▼
   ┌─────────┐      "وش تبي؟"
   │  Home   │──────────────────┐
   │ Screen  │                  │
   └────┬────┘                  │
        │                       │
   ┌────┴────┐            ┌────┴────┐
   │ Browse  │            │  Search │
   │ (تصفح)  │            │ (بحث AI)│
   └────┬────┘            └────┬────┘
        │                      │
        │  "أبي مطعم ياباني    │
        │   رخيص بالعليا"      │
        │                      │
        ▼                      ▼
   ┌─────────────────────────────┐
   │      نتائج (Results)        │
   │  - خريطة                    │
   │  - بطاقات أماكن             │
   │  - فلاتر (ميزانية/فئة)      │
   └──────────┬──────────────────┘
              │
              ▼
   ┌─────────────────────────────┐
   │     صفحة المكان (Detail)    │
   │  - صور حقيقية               │
   │  - أسعار المنيو بالريال     │
   │  - تقييم Google             │
   │  - 🛵 مقارنة التوصيل:       │
   │    هنقرستيشن: 43 ر.س        │
   │    جاهز: 45 ر.س              │
   │    كيتا: 43 ر.س (توصيل مجاني)│ ← الأرخص!
   │  - 📍 الموقع + خريطة        │
   │  - ⏰ أوقات العمل            │
   │  - "Perfect For": شباب 🔥   │
   └──────────┬──────────────────┘
              │
         ┌────┴────┐
         │ Actions │
         ├─────────┤
         │ ❤️ فضّل  │
         │ 🗺 وصلني │ (Google Maps)
         │ 📱 اطلب  │ (رابط التطبيق)
         │ 📤 شارك  │
         └─────────┘
```

---

## 📱 آلية التطبيق (iOS App Mechanism)

### Tech Stack

```
┌─────────────────────────────────────────┐
│              iOS App Stack               │
├─────────────────────────────────────────┤
│                                          │
│  UI Layer:                               │
│  ├── SwiftUI (iOS 17+)                  │
│  ├── MapKit (خرائط تفاعلية)              │
│  ├── CoreLocation (GPS)                  │
│  └── Liquid Glass Design (iOS 26 ready)  │
│                                          │
│  Data Layer:                             │
│  ├── Supabase Swift SDK (API + Auth)     │
│  ├── SwiftData (Offline cache)           │
│  ├── URLSession (Network)                │
│  └── Combine (Reactive data flow)        │
│                                          │
│  AI Layer:                               │
│  ├── NaturalLanguage (Arabic NLP)        │
│  ├── CreateML (On-device recommendations)│
│  └── Client-side search algorithms       │
│                                          │
│  Features:                               │
│  ├── Offline-first (SwiftData sync)      │
│  ├── Push Notifications (APNs)           │
│  ├── Widgets (StandBy, Lock Screen)      │
│  ├── App Intents (Siri shortcuts)        │
│  └── Share Extension                     │
│                                          │
└─────────────────────────────────────────┘
```

### Offline-First Architecture

```
┌──────────────────────────────────────────────┐
│              Offline-First Flow               │
│                                               │
│  1. أول فتح:                                  │
│     App → Supabase API → Download places.json │
│     → Store in SwiftData (local DB)           │
│                                               │
│  2. استخدام عادي:                              │
│     User searches → SwiftData (instant!)      │
│     Background: sync new data from API        │
│                                               │
│  3. بدون إنترنت:                               │
│     كل شي يشتغل! (cached data)               │
│     بس التوصيل والأسعار الجديدة ما تتحدث     │
│                                               │
│  4. رجع الإنترنت:                              │
│     Auto-sync → update local DB               │
│     → UI refreshes automatically              │
│                                               │
└──────────────────────────────────────────────┘
```

### من الموقع → التطبيق (Migration Path)

```
الحين (Static):                    المستقبل (Dynamic):
                                    
places.json ──────────────────→ PostgreSQL (Supabase)
  ↓                                  ↓
GitHub Pages ─────────────────→ API endpoints
  ↓                                  ↓
HTML/CSS/JS ──────────────────→ Website (API client)
                                     ↓
                               iOS App (Swift SDK)
                                     ↓
                               Android (later)

Migration Script:
1. places.json → SQL INSERT statements
2. delivery-prices.json → delivery_prices table
3. prices-*.json → menu_items table
4. Set up Supabase Edge Functions as API
5. Update website to call API instead of JSON
6. iOS app uses same API
```

---

## 🧴 مقارنات العطور (Perfume Comparison) — فئة جديدة!

### الفكرة
إضافة خانة "عطور" بالتطبيق — مقارنة أسعار العطور عبر المحلات

### البيانات المطلوبة

**محلات العطور بالرياض:**
- العربية للعود (Arabian Oud) — أكبر سلسلة
- عبد الصمد القرشي (Abdul Samad Al Qurashi)
- المعبود (Ahmed Al Maghribi)
- الماجد للعود (AlMajed Oud)
- رصاصي (Rasasi)
- سويس أرابيان (Swiss Arabian)
- Armaf
- Lattafa
- Paris Gallery
- Faces
- Sephora
- نيشان (Nishane)
- Fragrance Du Bois
- Xerjoff
- عطور شميم (Shameem)

**مقارنات:**
1. **سعر نفس العطر** عبر محلات مختلفة
2. **بدائل أرخص** (dupes/alternatives)
3. **تقييمات** من Fragrantica Arabia
4. **مكونات العطر** (notes)
5. **"شبيه بـ..."** — عطور رخيصة تشبه الغالية

### Schema

```json
{
  "perfume_shops": {
    "id": "uuid",
    "name_ar": "العربية للعود",
    "name_en": "Arabian Oud",
    "category": "عطور",
    "branches": [...],
    "website": "...",
    "price_range": "50-2000 SAR"
  },
  "perfumes": {
    "id": "uuid",
    "name": "Oud Ispahan",
    "brand": "Dior",
    "notes": ["oud", "rose", "amber"],
    "size_ml": 75,
    "prices": [
      {"shop": "Paris Gallery", "price": 1200},
      {"shop": "Faces", "price": 1150},
      {"shop": "Online", "price": 980}
    ],
    "alternatives": [
      {"name": "Lattafa Oud Mood", "price": 85, "similarity": "85%"}
    ],
    "fragrantica_rating": 4.2
  }
}
```

---

## 📊 كيف الناجحين بنوها (من الأبحاث)

### Zomato Architecture (أنجح تطبيق اكتشاف أماكن)

```
Frontend:  React Native (iOS + Android)
Backend:   Python (Django) + Java (Spring Boot)
Database:  PostgreSQL (sharded by region)
Cache:     Redis + Memcached
Events:    Apache Kafka (real-time orders/notifications)
Search:    Elasticsearch (smart search)
CDN:       Cloudflare/AWS CloudFront
Maps:      Google Maps API
Payments:  Stripe + PayU + Razorpay
Deploy:    Docker + Kubernetes (K8s)
AI:        OpenAI partnership (2026)
```

### Yelp Architecture

```
Frontend:  Native iOS (Swift) + Android (Kotlin)
Backend:   Python + Java microservices
Database:  MySQL (sharded) + PostgreSQL
Cache:     Memcached
Search:    Elasticsearch (custom ranking)
Events:    Apache Kafka
AI:        Custom LLMs for search + review summaries
Deploy:    AWS + Docker
Special:   CHAOS (Server-Driven UI framework)
```

### ما ناخذ منهم لحجمنا (Startup):

```
إحنا (MVP):                      هم (at scale):
                                   
SwiftUI (native)          ←──── React Native / Native
Supabase (PostgreSQL)     ←──── PostgreSQL (sharded)
Supabase Edge Functions   ←──── Microservices
Supabase Realtime         ←──── Apache Kafka
PostgreSQL FTS            ←──── Elasticsearch
Cloudflare CDN            ←──── AWS CloudFront
On-device AI              ←──── Server-side AI
GitHub Actions            ←──── Jenkins/custom CI
$0/month                  ←──── $100K+/month
```

**المبدأ:** نبدأ بسيط + نكبر لما نحتاج. Supabase يتحول لـ microservices لما نوصل 100K+ users.

---

## 🔧 الخطوات العملية (Action Items)

### الآن (اليوم):
- [x] ✅ تعريف MVP واضح
- [x] ✅ اختيار البنية التحتية (Supabase)
- [x] ✅ تصميم Data Flow
- [ ] 🔄 جمع بيانات شامل (5 وكلاء شغالين)
- [ ] 🔄 خطة سيرفر مفصلة (وكيل شغال)

### القريب:
- [ ] إنشاء Supabase project
- [ ] Migration script: JSON → PostgreSQL
- [ ] API endpoints (Edge Functions)
- [ ] تحديث الموقع ليستخدم API
- [ ] Xcode project + Swift models

### بعدها:
- [ ] AI search engine (Arabic NLP)
- [ ] بيانات عطور
- [ ] iOS MVP على TestFlight
- [ ] Domain + Analytics

---

## 📈 مقاييس النجاح (MVP Success Metrics)

| المقياس | الهدف |
|---------|-------|
| أماكن | 5,000+ |
| أسعار حقيقية | 500+ مكان |
| مقارنة توصيل | 200+ مطعم × 6 تطبيقات |
| محلات عطور | 50+ |
| وقت البحث | < 200ms |
| حجم التطبيق | < 30MB |
| Offline | يشتغل بدون إنترنت |

---

*هذا الـ MVP. واضح. محدد. قابل للتنفيذ. يلا نبني.*
